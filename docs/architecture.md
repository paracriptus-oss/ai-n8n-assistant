# Архитектура проекта

Проект **AI n8n Assistant** представляет собой систему автоматизированной обработки данных с использованием **платформы n8n** и **LLM (Large Language Model)**.  
Цель — создание универсального шаблона для интеграции искусственного интеллекта в процессы обработки текстовых запросов, анализа и генерации ответов.

---

## 🧠 Общая логика работы

1. **Входные данные (Input)**  
   Пользователь или внешний сервис передаёт запрос (например, текст, JSON или сообщение из чата).

2. **Предобработка (Preprocess)**  
   n8n нормализует данные:  
   - удаляет лишние символы,  
   - добавляет системные параметры,  
   - подготавливает данные к отправке в LLM.

3. **Запрос к модели (LLM Request)**  
   Workflow отправляет подготовленный текст в модель (например, OpenAI API).  
   Полученный ответ сохраняется в переменные сценария.

4. **Постобработка (Postprocess)**  
   Выполняется очистка и форматирование ответа, проверка структуры JSON (если нужно).

5. **Обработка ошибок (Error Handling)**  
   При сбое выполняется отдельная ветка — логирование и уведомление в Telegram или Slack.

6. **Результат (Output)**  
   Обработанные данные возвращаются в n8n, отправляются пользователю или в следующую систему.

---

## ⚙️ Компоненты проекта

| Компонент | Назначение |
|------------|------------|
| **n8n Workflow** | Основная логика автоматизации. Управляет потоком данных и обращениями к LLM. |
| **LLM API** | Генерация ответов, анализ текста и выполнение интеллектуальных задач. |
| **Docs** | Документация и схемы, поясняющие структуру проекта. |
| **Workflows** | JSON-файлы экспортированных сценариев n8n. |

---

## 📦 Структура проекта

```text
ai-n8n-assistant/
├── docs/                     # Документация проекта
│   ├── architecture.md       # Описание архитектуры
│   └── diagrams.md           # Визуальные схемы (Mermaid)
├── workflows/                # Экспорты n8n-сценариев (JSON)
│   ├── 01_ai_assistant_base.json
│   ├── ai_assistant_workflow.json
│   └── README.md
├── screenshots/              # Скриншоты (заглушка)
│   └── .gitkeep
├── examples/                 # Примеры входных данных
│   └── input_resume_sample.txt
├── LICENSE
├── .gitignore
└── README.md
```
