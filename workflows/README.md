# ⚙️ Workflows — Экспорты n8n

В этой папке находятся **JSON-файлы экспортированных сценариев n8n**, реализующих различные функции AI-ассистента.

---

## 📘 Назначение

Эти сценарии служат готовыми шаблонами для автоматизации процессов с использованием **n8n** и **LLM (Large Language Models)**.  
Каждый файл можно импортировать напрямую в n8n и использовать в собственных проектах.

---

## 📂 Сценарии

| Файл | Описание |
|------|-----------|
| **01_ai_assistant_base.json** | Базовый сценарий AI-помощника. Содержит обработку входных данных, запрос к LLM и возврат результата. |
| **ai_assistant_workflow.json** | Расширенный вариант с дополнительными шагами: логирование, постобработка и уведомления. |

---

## 🚀 Импорт в n8n

1. Открой **n8n** → вкладку **Workflows**.  
2. Нажми **Import from File**.  
3. Выбери нужный `.json` из этой папки.  
4. Проверь переменные окружения и настройки подключения к API (например, OpenAI).  
5. Сохрани и запусти сценарий.

---

## 💡 Рекомендации

- Перед запуском укажи свои ключи API в настройках переменных `OPENAI_API_KEY` или аналогичных.  
- Можно создавать собственные сценарии на основе этих шаблонов.  
- Рекомендуется хранить персональные сценарии отдельно — например, в `/custom_workflows/`.
