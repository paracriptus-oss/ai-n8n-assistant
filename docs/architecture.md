# 🧠 Архитектура AI n8n Assistant

Этот документ описывает архитектуру проекта **AI n8n Assistant** — гибкой системы автоматизации, объединяющей **n8n**, **LLM-модели** и **API-интеграции**.

---

## 🧩 Основные компоненты

| Компонент | Описание |
|-----------|----------|
| **Frontend (UI)** | Визуальный интерфейс n8n для настройки сценариев и контроля потоков. |
| **Backend (Core)** | Исполнение workflow, управление данными, подключение API-ключей. |
| **LLM API Layer** | Взаимодействие с моделями (OpenAI/др.) через HTTP/REST. |
| **Data Processing** | Подготовка, фильтрация и структурирование данных до/после вызова модели. |
| **Storage / Logs** | Хранение результатов, логов и отладочной информации. |

---

## 🔗 Визуальные схемы

Диаграммы потоков данных и архитектуры можно посмотреть в отдельном файле:  
👉 [**diagrams.md**](diagrams.md)

---

## 🔌 Взаимодействие компонентов

1. **n8n** управляет потоком данных, вызывает API и обрабатывает результаты.  
2. **LLM-модель** выполняет анализ, генерацию текста или принятие решений.  
3. **Промежуточные узлы** (`Function`, `Set`, `If`, `HTTP Request`) формируют бизнес-логику.  
4. **Результаты** сохраняются или передаются в сторонние сервисы (Telegram, Notion, Google Sheets и т. д.).

---

## 🧱 Преимущества архитектуры

- Простая интеграция новых API и моделей.  
- Гибкость сценариев без изменения ядра.  
- Прозрачные зависимости и структура.  
- Масштабирование под разные задачи.
