# Экспорты n8n (Workflows)

В этой директории хранятся экспортированные сценарии n8n в формате JSON.  
Каждый файл представляет собой автономный workflow, который можно импортировать в n8n через интерфейс:

> **n8n → Import from File → выбрать нужный JSON**

---

## 📂 Содержимое

| Файл | Назначение |
|------|-------------|
| `01_ai_assistant_base.json` | Базовый сценарий обработки запроса и ответа от LLM |
| `ai_assistant_workflow.json` | Расширенный вариант с логированием и обработкой ошибок |

---

## ⚙️ Использование

1. Скачайте нужный файл из этой папки.  
2. Импортируйте его в n8n.  
3. Настройте API-ключи и переменные окружения при необходимости.  
4. Запустите workflow и протестируйте результат.

---

🧩 **Примечание:**  
Файл `.gitkeep` используется только для сохранения структуры репозитория, если папка временно пуста.
